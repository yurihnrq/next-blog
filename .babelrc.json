// Configuration to run styled-components in server side.
// babel-plugin-styled-components must be installed as dev dependency.
{
  "presets": [
    "next/babel"
  ],
  "plugins": [
    [
      "styled-components",
      {
        "ssr": true
      }
    ]
  ]
}